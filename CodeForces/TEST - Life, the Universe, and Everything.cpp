#include <bits/stdc++.h>
#define cpu() ios::sync_with_stdio(false);cin.tie(nullptr);
using namespace std;
typedef long long ll;
const ll mod = 1e9+7;
void solve()
{
    
}
int main()
{
    int n;
    while (cin >> n)
    {
        if(n != 42) {
            cout << n << endl;
            continue;
        }
        break;
    }
    return 0;
}